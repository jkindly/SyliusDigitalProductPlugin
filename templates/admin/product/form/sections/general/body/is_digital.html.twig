{% set form = hookable_metadata.context.form %}
{% set product = hookable_metadata.context.resource %}

{% if product.isSimple %}
    {{ form_row(form.variant.isDigital, {'label': 'sylius_digital_product.ui.is_digital_product'}) }}
{% endif %}
