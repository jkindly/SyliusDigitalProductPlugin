{% set order = hookable_metadata.context.order %}

<tbody>
    {% for file in order.files %}
        <tr {{ sylius_test_html_attribute('product-row', file.id) }}>
            {% hook 'body' with { file } %}
        </tr>
    {% endfor %}
</tbody>
